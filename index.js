(function(){
//   var PIXI = require("pixi.js");
/*   var mapgen = require("./js/mapgen.js");
   var utility = require("./js/utility.js");
   var vector = require("./js/vector.js");
   var main_canvas = require("./js/main_canvas.js");
   var tiles_inter =  require("./js/tiles_inter.js");
   var mapgen = require("./js/mapgen.js");
   var main = mapgen.main;

   var renderer = new PIXI.CanvasRenderer(window.innerWidth, window.I);

   document.body.appendChild(renderer.view);

   var stage = new PIXI.Container();

   PIXI.loader.add("dude","./assets/dude.png").load( function(loader, resources) {
      // This creates a texture from a 'dude.png' image.
      var dude = new PIXI.Sprite(resources.dude.texture);

      // Setup the position and scale of the bunny
      dude.position.x = 400;
      dude.position.y = 300;

      dude.scale.x = 2;
      dude.scale.y = 2;

      // Add the bunny to the scene we are building.
      stage.addChild(dude);

      // kick off the animation loop (defined below)
      animate(dude);
    });*/

})();

